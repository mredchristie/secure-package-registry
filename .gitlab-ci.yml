stages:
  - check

image: golang:1.25.6

check:
  stage: check
  script:
    - apt update
    - apt install -y just unzip
    - curl -fsSL https://bun.sh/install | bash
    - export PATH="$HOME/.bun/bin:$PATH"
    - just check
